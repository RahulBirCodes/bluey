# core
numpy>=1.24
torch            # installed alongside torch_xla on TPU VM; leave version pinning to TPU wheels
torch_xla[tpu]   # same note as above; comes from libtpu GCS index

# training + logging
wandb>=0.17

# optional niceties
tqdm>=4.66
